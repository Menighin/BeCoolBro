<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/zen_card_zoom_root"
    android:fitsSystemWindows="true"
    tools:context="com.onsoftwares.zensource.activities.ZenCardZoomActivity"
    android:onClick="fullView">

    <include
        layout="@layout/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <ImageView
        android:id="@+id/zen_card_zoom_img"
        android:transitionName="@string/transition_zoom_card"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</RelativeLayout>
